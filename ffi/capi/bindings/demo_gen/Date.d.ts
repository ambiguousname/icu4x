import { Calendar } from "../Calendar.mjs"
import { Date } from "../Date.mjs"
import { Locale } from "../Locale.mjs"
export function monthCode(year: number, month: number, day: number, provider: DataProvider, name: string);
export function era(year: number, month: number, day: number, provider: DataProvider, name: string);
